import React from 'react';
import './Catalog.scss';
import { Link } from 'react-router-dom';

const Catalog = ({ data }) => {
  console.log(data)
  return(
    <h2>Catelog Start</h2>
  )
}

export default Catalog;